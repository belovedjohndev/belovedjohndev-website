<section class="why-me section-padding" id="why-me">
  <div class="container">
    <div class="section-title">
      <p class="section-eyebrow">Why clients choose this approach</p>
      <h2>Clearer communication, better workflow thinking, less agency overhead</h2>
      <p>
        The value here is not just coding. It is translating messy operations into software that is practical to buy,
        practical to build, and practical to use after launch.
      </p>
    </div>

    <div class="why-me-layout">
      <div class="why-me-grid">
        <article class="why-me-card">
          <p class="why-me-card__label">Direct access</p>
          <h3>You work directly with the builder</h3>
          <p>
            That keeps communication faster, scope discussions clearer, and decisions grounded in the real business and delivery tradeoffs.
          </p>
        </article>

        <article class="why-me-card">
          <p class="why-me-card__label">Workflow-first thinking</p>
          <h3>The business process shapes the software</h3>
          <p>
            I design around roles, approvals, reporting needs, and handoffs rather than forcing the business into a generic product shape.
          </p>
        </article>

        <article class="why-me-card">
          <p class="why-me-card__label">Maintainability</p>
          <h3>Architecture choices stay grounded</h3>
          <p>
            Security, access control, and practical delivery choices matter from day one when the software touches daily operations.
          </p>
        </article>

        <article class="why-me-card">
          <p class="why-me-card__label">Delivery discipline</p>
          <h3>Scope is shaped around the first useful version</h3>
          <p>
            I prefer milestones, real priorities, and removing the biggest operational bottleneck before expanding into lower-value extras.
          </p>
        </article>
      </div>

      <aside class="why-me-panel">
        <p class="why-me-card__label why-me-card__label--light">Working style</p>
        <h3>What that means for a client in practice</h3>
        <ul class="check-list">
          <li>Clear fit feedback before the build starts</li>
          <li>Direct updates instead of handoff confusion</li>
          <li>Phased delivery when the project needs it</li>
          <li>Support after launch when the workflow goes live</li>
          <li>More serious operational framing than a standard freelancer portfolio</li>
        </ul>
      </aside>
    </div>
  </div>
</section>

<style>
  .why-me {
    background-color: var(--color-white);
  }

  .section-eyebrow,
  .why-me-card__label {
    font-size: 0.82rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .section-eyebrow,
  .why-me-card__label {
    color: var(--color-primary);
  }

  .why-me-card__label--light {
    color: var(--color-accent);
  }

  .why-me-layout {
    display: grid;
    grid-template-columns: minmax(0, 1.3fr) minmax(300px, 0.78fr);
    gap: var(--space-lg);
  }

  .why-me-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: var(--space-lg);
  }

  .why-me-card,
  .why-me-panel {
    padding: var(--space-lg);
    border-radius: var(--border-radius);
    border: 1px solid rgba(38, 70, 83, 0.08);
    box-shadow: var(--shadow-md);
  }

  .why-me-card {
    background-color: var(--color-white);
    transition: var(--transition);
  }

  .why-me-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
  }

  .why-me-card p:last-child,
  .why-me-panel li {
    margin-bottom: 0;
    color: var(--color-text);
    line-height: 1.75;
  }

  .why-me-panel {
    background:
      radial-gradient(circle at top right, rgba(240, 188, 116, 0.12), transparent 30%),
      linear-gradient(135deg, rgba(16, 32, 51, 0.98), rgba(23, 50, 74, 0.96));
  }

  .why-me-panel h3,
  .why-me-panel li {
    color: var(--color-white);
  }

  .check-list {
    list-style: none;
    display: grid;
    gap: var(--space-sm);
    margin-top: var(--space-md);
  }

  .check-list li {
    position: relative;
    padding-left: var(--space-md);
  }

  .check-list li::before {
    content: '+';
    position: absolute;
    left: 0;
    color: var(--color-accent);
    font-weight: 700;
  }

  @media (max-width: 960px) {
    .why-me-layout,
    .why-me-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
