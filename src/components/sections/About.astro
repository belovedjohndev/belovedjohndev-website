<section class="about section-padding" id="about">
  <div class="container">
    <div class="about-grid">
      <img
        src="/assets/images/beloved-john.webp"
        alt="Beloved John - Web Developer based in Cambodia"
        width="300"
        height="300"
        loading="lazy"
        class="about-image"
        onerror="this.style.display='none'; this.nextElementSibling.style.display='block';"
      />
      <div class="image-placeholder" style="display: none; padding: 2rem; background: var(--color-light); text-align: center; color: var(--color-text-muted); border-radius: var(--border-radius);">
        Photo unavailable
      </div>

      <div class="about-content">
        <h2>About Me</h2>
        <p>
          I'm a freelance web developer based in Cambodia, specializing in building
          fast, accessible, and maintainable websites for small businesses.
        </p>
        <p>
          I work directly with clients—no agencies, clear communication, and
          transparent pricing. Every project is based on needs with attention to
          performance, SEO, and user experience.
        </p>

        <div class="skills">
          <h3>Technologies & Tools</h3>
          <ul class="skills-list">
            <li>Next.js, TypeScript, Tailwind CSS, HTML5, CSS3, JavaScript (ES6+)</li>
            <li>Astro, React, Django, Node.js</li>
            <li>Responsive Design, Mobile-First</li>
            <li>Performance Optimization</li>
            <li>SEO & Accessibility</li>
            <li>Git, GitHub</li>
            <li>The technology stack is selected based on the problem being addressed, scalability needs, performance requirements, and the most appropriate solution for the project.</li>
          </ul>
        </div>

        <div class="about-links">
          <a href="https://github.com/belovedjohndev" target="_blank" rel="noopener" class="btn btn-dark">
            View GitHub
          </a>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .about {
    background-color: var(--color-white);
  }

  .about-grid {
    display: grid;
    grid-template-columns: 300px 1fr;
    gap: var(--space-xl);
    align-items: start;
  }

  .about-image {
    border-radius: var(--border-radius);
    width: 100%;
    max-width: 300px;
    height: auto;
    box-shadow: var(--shadow-md);
  }

  .about-content h2 {
    margin-bottom: var(--space-md);
  }

  .about-content p {
    margin-bottom: var(--space-sm);
  }

  .skills {
    margin-top: var(--space-lg);
  }

  .skills h3 {
    font-size: 1.25rem;
    margin-bottom: var(--space-sm);
  }

  .skills-list {
    list-style: none;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: var(--space-xs);
    margin-top: var(--space-sm);
  }

  .skills-list li {
    padding-left: var(--space-md);
    position: relative;
    color: var(--color-text-muted);
  }

  .skills-list li::before {
    content: '▸';
    position: absolute;
    left: 0;
    color: var(--color-primary);
  }

  .about-links {
    margin-top: var(--space-lg);
  }

  @media (max-width: 768px) {
    .about-grid {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .about-image {
      margin: 0 auto;
    }

    .skills-list {
      grid-template-columns: 1fr;
      text-align: left;
    }
  }
</style>
