<section class="hero" id="top">
  <div class="container hero-content">
    <h1>Web Developer Building Fast, Professional Websites</h1>
    <p class="hero-subtitle">
      I build mobile-first websites for small businesses and entrepreneurs.
      Focused on performance, accessibility, and clean code.
    </p>
    <p class="hero-location">Based in Cambodia · Working with clients worldwide</p>
    
    <div class="hero-buttons">
      <a href="#contact" class="btn btn-primary">Get in Touch</a>
      <a href="#projects" class="btn btn-secondary">View Projects</a>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    overflow: hidden;
    color: var(--color-white);
    padding: 6rem 0 4rem;
    background: linear-gradient(rgba(15, 23, 42, 0.85), rgba(15, 23, 42, 0.85)),
      url('/assets/images/hero-bg.webp');
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
  }

  .hero::after {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at center, rgba(42, 157, 143, 0.15), transparent 60%);
    pointer-events: none;
    animation: pulse 4s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% {
      opacity: 1;
    }
    50% {
      opacity: 0.8;
    }
  }

  .hero-content {
    position: relative;
    z-index: 2;
  }

  .hero h1 {
    margin-bottom: var(--space-md);
    color: var(--color-white);
    max-width: 680px;
  }

  .hero-subtitle {
    font-size: clamp(1.1rem, 2vw, 1.2rem);
    opacity: 0.95;
    max-width: 620px;
    margin-bottom: var(--space-sm);
    color: var(--color-white);
    line-height: 1.7;
  }

  .hero-location {
    font-size: 0.95rem;
    opacity: 0.85;
    margin-bottom: var(--space-lg);
    color: var(--color-white);
    display: flex;
    align-items: center;
    gap: var(--space-xs);
  }

  .hero-location::before {
    font-size: 1rem;
  }

  .hero-buttons {
    display: flex;
    gap: var(--space-md);
    margin-top: var(--space-lg);
    flex-wrap: wrap;
  }

  .hero-buttons .btn {
    min-width: 160px;
  }

  @media (max-width: 768px) {
    .hero {
      padding: 5rem 0 3rem;
    }

    .hero-buttons {
      flex-direction: column;
      align-items: flex-start;
      width: 100%;
    }

    .hero-buttons .btn {
      width: 100%;
    }
  }
</style>
