<section class="hero" id="top">
  <div class="container hero-content">
    <h1>
      Independent Digital Systems Developer<br />
      <span class="hero-highlight">Building Practical Tools for Real Businesses</span>
    </h1>

    <p class="hero-subtitle">
      I design and build real-world digital systems for businesses —
      from management and POS systems to automation tools, internal platforms,
      and reliable business websites when needed.
    </p>

    <p class="hero-location">
      Based in Cambodia · Working directly with local businesses
    </p>

<div class="hero-buttons">
  <a href="https://websitedemo-drab.vercel.app/" 
     class="btn btn-primary" 
     target="_blank" 
     rel="noopener noreferrer">
    View Demo
  </a>

  <a href="#contact" class="btn btn-secondary">
    Contact Me
  </a>
</div>

</section>


<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    overflow: hidden;
    color: var(--color-white);
    padding: 6rem 0 4rem;
    background: linear-gradient(rgba(15, 23, 42, 0.85), rgba(15, 23, 42, 0.85)),
      url('/assets/images/hero-bg.webp');
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
  }

  .hero::after {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(
      circle at center,
      rgba(42, 157, 143, 0.15),
      transparent 60%
    );
    pointer-events: none;
    animation: pulse 4s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.85; }
  }

  .hero-content {
    position: relative;
    z-index: 2;
    max-width: 720px;
  }

  .hero h1 {
    margin-bottom: var(--space-md);
    color: var(--color-white);
    line-height: 1.2;
  }

  .hero-highlight {
    display: block;
    color: var(--color-accent);
    font-weight: 600;
  }

  .hero-subtitle {
    font-size: clamp(1.1rem, 2vw, 1.2rem);
    opacity: 0.95;
    margin-bottom: var(--space-sm);
    line-height: 1.7;
  }

  .hero-location {
    font-size: 0.95rem;
    opacity: 0.85;
    margin-bottom: var(--space-lg);
  }

  .hero-buttons {
    display: flex;
    gap: var(--space-md);
    margin-top: var(--space-lg);
    flex-wrap: wrap;
  }

  .hero-buttons .btn {
    min-width: 160px;
  }

  @media (max-width: 768px) {
    .hero {
      padding: 5rem 0 3rem;
    }

    .hero-buttons {
      flex-direction: column;
      align-items: flex-start;
      width: 100%;
    }

    .hero-buttons .btn {
      width: 100%;
    }
  }
</style>
