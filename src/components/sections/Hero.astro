<section class="hero" id="top">
  <div class="container hero-content">
    <p class="hero-eyebrow">Independent business systems developer</p>
    <h1>
      Secure, maintainable systems for businesses that need software to do real work.
    </h1>

    <p class="hero-subtitle">
      I work directly with founders and operators to build custom business systems,
      operational websites, and automation tools that improve day-to-day execution
      without adding unnecessary complexity.
    </p>

    <p class="hero-location">
      Built for international clients who need practical delivery, clear communication,
      and systems they can trust with business-critical work.
    </p>

    <div class="hero-buttons">
      <a href="#contact" class="btn btn-primary">
        Discuss Your Project
      </a>

      <a href="#projects" class="btn btn-secondary">
        Review Selected Work
      </a>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    overflow: hidden;
    color: var(--color-white);
    padding: 6rem 0 4rem;
    background: linear-gradient(rgba(15, 23, 42, 0.85), rgba(15, 23, 42, 0.85)),
      url('/assets/images/hero-bg.webp');
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
  }

  .hero::after {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(
      circle at center,
      rgba(42, 157, 143, 0.15),
      transparent 60%
    );
    pointer-events: none;
    animation: pulse 4s ease-in-out infinite;
  }

  @keyframes pulse {
    0%,
    100% {
      opacity: 1;
    }

    50% {
      opacity: 0.85;
    }
  }

  .hero-content {
    position: relative;
    z-index: 2;
    max-width: 760px;
  }

  .hero-eyebrow {
    display: inline-flex;
    align-items: center;
    font-size: 0.85rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--color-accent);
    margin-bottom: var(--space-sm);
  }

  .hero h1 {
    margin-bottom: var(--space-md);
    color: var(--color-white);
    line-height: 1.2;
  }

  .hero-subtitle {
    font-size: clamp(1.1rem, 2vw, 1.2rem);
    opacity: 0.95;
    margin-bottom: var(--space-sm);
    line-height: 1.7;
  }

  .hero-location {
    font-size: 0.95rem;
    opacity: 0.85;
    margin-bottom: var(--space-lg);
    line-height: 1.7;
    max-width: 640px;
  }

  .hero-buttons {
    display: flex;
    gap: var(--space-md);
    margin-top: var(--space-lg);
    flex-wrap: wrap;
  }

  .hero-buttons .btn {
    min-width: 180px;
  }

  @media (max-width: 768px) {
    .hero {
      min-height: auto;
      padding: 5rem 0 3rem;
    }

    .hero-buttons {
      flex-direction: column;
      align-items: flex-start;
      width: 100%;
    }

    .hero-buttons .btn {
      width: 100%;
    }
  }
</style>
